rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Só permite ler/escrever se o usuário estiver logado
      allow read, write: if request.auth != null;
    }
  }
}